<%= shout_form_for(TextShout) do |form| %>
  <%= form.text_field :body, placeholder: "Shout Here!" %>
<% end %>

<%= shout_form_for(TextShout) do |form| %>
  <%= form.text_field :body, placeholder: "Shout Here!" %>
<% end %>

<%= render @dashboard.shouts %>
